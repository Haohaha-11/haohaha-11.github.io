# Hugo 网站配置指南

本文档详细说明如何修改你的 Hugo 学术网站的各项配置。

---

## 📋 目录

1. [基本信息配置](#基本信息配置)
2. [个人资料配置](#个人资料配置)
3. [菜单导航配置](#菜单导航配置)
4. [社交链接配置](#社交链接配置)
5. [主题外观配置](#主题外观配置)
6. [数学公式配置](#数学公式配置)
7. [内容管理](#内容管理)
8. [自定义样式](#自定义样式)
9. [部署配置](#部署配置)

---

## 基本信息配置

**文件位置：** `hugo.toml`

### 网站基本信息

```toml
baseURL = "/"                          # 网站根URL，本地开发用 "/"，部署时改为实际域名
languageCode = "zh-cn"                 # 语言代码
title = "Hao Jiang's Page"            # 浏览器标签页显示的标题
theme = "heyo"                         # 使用的主题名称
defaultContentLanguage = "zh-cn"       # 默认内容语言
```

**修改示例：**
```toml
baseURL = "https://yourusername.github.io/"  # 部署到 GitHub Pages 时
title = "张三的学术主页"                      # 改成你的名字
```

### 网站功能开关

```toml
summarylength = 100        # 文章摘要长度（字符数）
enableEmoji = true         # 启用 Emoji 表情支持
enableRobotsTXT = true     # 生成 robots.txt 文件
enableGitInfo = true       # 启用 Git 信息（显示最后修改时间）
```

### 分页设置

```toml
[pagination]
disableAliases = false     # 是否禁用分页别名
pagerSize = 10             # 每页显示的文章数量
path = ''                  # 分页路径
```

**修改建议：**
- 如果文章很多，可以将 `pagerSize` 改为 `15` 或 `20`

---

## 个人资料配置

**文件位置：** `hugo.toml`

### 作者信息

```toml
[params.author]
name = "Hao Jiang"                    # 你的名字
email = "your.email@example.com"      # 你的邮箱
```

### 个人简介

```toml
[params]
title = "Hao Jiang"                   # 侧边栏显示的名字
description = "学术研究者 | 博士研究生 | 人工智能"  # 名字下方的描述
profilePicture = "img/avatar.jpg"     # 头像图片路径（相对于 static/ 目录）
```

**修改步骤：**

1. **更改名字和描述：**
   ```toml
   title = "张三"
   description = "计算机科学博士 | 机器学习研究者"
   ```

2. **更换头像：**
   - 将你的头像图片放到 `static/img/` 目录
   - 命名为 `avatar.jpg`（或其他名字）
   - 如果用其他名字，修改配置：
     ```toml
     profilePicture = "img/my-photo.png"
     ```

### 侧边栏引用语

**文件位置：** `layouts/partials/sidebar.html`

找到这一行：
```html
<p class="sidebar-quote">站在能分割世界的桥</p>
```

**修改为你喜欢的句子：**
```html
<p class="sidebar-quote">你的座右铭或喜欢的句子</p>
```

---

## 菜单导航配置

**文件位置：** `hugo.toml`

### 当前菜单结构

```toml
[[menu.main]]
name = "首页"              # 菜单显示的文字
pageRef = "/"             # 链接地址
weight = 1                # 排序权重（数字越小越靠前）

[[menu.main]]
name = "博文"
pageRef = "/posts/"
weight = 2

[[menu.main]]
name = "讲义"
pageRef = "/notes/"
weight = 3

[[menu.main]]
name = "论文"
pageRef = "/papers/"
weight = 4

[[menu.main]]
name = "关于"
pageRef = "/about/"
weight = 5
```

### 修改菜单

**添加新菜单项：**
```toml
[[menu.main]]
name = "项目"
pageRef = "/projects/"
weight = 6
```

**删除菜单项：**
直接删除对应的 `[[menu.main]]` 块

**修改菜单顺序：**
调整 `weight` 值，数字越小越靠前

**修改菜单文字：**
```toml
name = "我的博客"    # 将"博文"改为"我的博客"
```

---

## 社交链接配置

**文件位置：** `hugo.toml`

### 当前社交链接

```toml
[[params.socialIcons]]
icon = "fab fa-github"                                    # Font Awesome 图标
title = "GitHub"                                          # 鼠标悬停提示
url = "https://github.com/yourusername"                   # 链接地址

[[params.socialIcons]]
icon = "fas fa-envelope"
title = "Email"
url = "mailto:your.email@example.com"

[[params.socialIcons]]
icon = "fab fa-google"
title = "Google Scholar"
url = "https://scholar.google.com/citations?user=YOUR_ID"

[[params.socialIcons]]
icon = "fab fa-linkedin"
title = "LinkedIn"
url = "https://linkedin.com/in/yourprofile"
```

### 修改社交链接

**更新链接地址：**
```toml
[[params.socialIcons]]
icon = "fab fa-github"
title = "GitHub"
url = "https://github.com/zhangsan"    # 改成你的 GitHub 用户名
```

**添加新的社交平台：**

常用图标：
- Twitter/X: `fab fa-twitter` 或 `fab fa-x-twitter`
- 知乎: `fab fa-zhihu`
- 微博: `fab fa-weibo`
- ORCID: `fab fa-orcid`
- ResearchGate: `fab fa-researchgate`
- 个人网站: `fas fa-globe`

示例：
```toml
[[params.socialIcons]]
icon = "fab fa-twitter"
title = "Twitter"
url = "https://twitter.com/yourusername"

[[params.socialIcons]]
icon = "fab fa-orcid"
title = "ORCID"
url = "https://orcid.org/0000-0000-0000-0000"
```

**删除社交链接：**
直接删除对应的 `[[params.socialIcons]]` 块

**查找更多图标：**
访问 [Font Awesome 图标库](https://fontawesome.com/icons) 搜索图标

---

## 主题外观配置

**文件位置：** `hugo.toml`

### 显示模式

```toml
displayMode = "auto"    # 可选值: "light" (浅色), "dark" (深色), "auto" (自动)
```

**选项说明：**
- `"light"` - 始终使用浅色模式
- `"dark"` - 始终使用深色模式
- `"auto"` - 根据系统设置自动切换

### 自定义 CSS/JS

```toml
customCss = ["css/custom.css"]    # 自定义 CSS 文件列表
customJs = []                     # 自定义 JavaScript 文件列表
```

**添加更多自定义样式：**
```toml
customCss = ["css/custom.css", "css/my-style.css"]
```

### Favicon（网站图标）

```toml
favicon = "favicons/"    # favicon 文件夹路径
```

**设置 favicon：**
1. 准备你的网站图标（建议使用 favicon 生成器）
2. 将生成的文件放到 `static/favicons/` 目录
3. 确保包含这些文件：
   - `favicon.ico`
   - `favicon-16x16.png`
   - `favicon-32x32.png`
   - `apple-touch-icon.png`

---

## 数学公式配置

**文件位置：** `hugo.toml`

```toml
[params.math]
enable = true           # 是否启用数学公式支持
use = "mathjax"        # 可选值: "mathjax" 或 "katex"
```

**选择渲染引擎：**
- `"mathjax"` - 功能更全面，支持更多 LaTeX 命令
- `"katex"` - 渲染速度更快，体积更小

**在文章中使用数学公式：**

行内公式：
```markdown
这是一个行内公式 $E = mc^2$
```

独立公式块：
```markdown
$$
\frac{\partial u}{\partial t} = c^2 \frac{\partial^2 u}{\partial x^2}
$$
```

---

## 内容管理

### 创建新内容

**创建博文：**
```bash
hugo new posts/my-new-post.md
```

**创建讲义：**
```bash
hugo new notes/lecture-02.md
```

**创建论文：**
```bash
hugo new papers/my-paper.md
```

### 文章 Front Matter（元数据）

每篇文章开头的配置：

```yaml
---
title: "文章标题"
date: 2024-01-01
draft: false                    # false=发布, true=草稿
tags: ["标签1", "标签2"]
categories: ["分类"]
math: true                      # 是否启用数学公式
showToc: true                   # 是否显示目录
summary: "文章摘要"              # 可选，不写则自动生成
---
```

**常用配置项：**

```yaml
---
title: "深度学习入门"
date: 2024-02-06
draft: false
tags: ["机器学习", "深度学习", "Python"]
categories: ["技术博客"]
math: true
showToc: true
summary: "本文介绍深度学习的基本概念和常用框架"
---
```

### 内容目录结构

```
content/
├── _index.md           # 首页内容（可选）
├── about.md            # 关于页面
├── posts/              # 博文目录
│   ├── _index.md       # 博文列表页配置
│   ├── post-1.md
│   └── post-2.md
├── notes/              # 讲义目录
│   ├── _index.md
│   └── lecture-01.md
└── papers/             # 论文目录
    ├── _index.md
    └── paper-1.md
```

### 修改"关于"页面

**文件位置：** `content/about.md`

直接编辑这个文件，使用 Markdown 格式：

```markdown
---
title: "关于我"
---

## 个人简介

我是一名博士研究生，研究方向为...

## 研究方向

- 深度学习
- 计算机视觉
- 自然语言处理

## 教育背景

- **博士** - 计算机科学，[大学名称]，2021-至今
- **硕士** - 人工智能，[大学名称]，2019-2021

## 发表论文

1. 论文标题 1
   作者列表
   会议/期刊名称，年份
   [PDF] [Code]

## 联系方式

- Email: your.email@example.com
- GitHub: [@yourusername](https://github.com/yourusername)
```

---

## 自定义样式

**文件位置：** `static/css/custom.css`

### 当前自定义样式

```css
/* 个人描述样式 */
.profile-description {
    font-size: 0.9rem;
    color: var(--text-secondary);
    /* ... */
}

/* 侧边栏导航菜单 */
.sidebar-nav a {
    font-size: 1.05rem;    /* 菜单字体大小 */
    padding: 0.7rem 1rem;  /* 菜单内边距 */
    /* ... */
}

/* 侧边栏引用语 */
.sidebar-quote {
    font-size: 0.9rem;     /* 引用语字体大小 */
    font-style: italic;    /* 斜体 */
    /* ... */
}
```

### 常见样式修改

**调整菜单字体大小：**
```css
.sidebar-nav a {
    font-size: 1.1rem;    /* 改为更大的字体 */
}
```

**修改引用语样式：**
```css
.sidebar-quote {
    font-size: 1rem;
    font-style: normal;    /* 取消斜体 */
    font-weight: bold;     /* 加粗 */
    color: #ff6b6b;       /* 改变颜色 */
}
```

**调整描述文字：**
```css
.profile-description {
    font-size: 1rem;       /* 更大的字体 */
    opacity: 1;            /* 完全不透明 */
}
```

**添加自定义颜色：**
```css
/* 自定义主题色 */
:root {
    --my-accent-color: #3498db;
}

.sidebar-nav a.active {
    background-color: var(--my-accent-color);
}
```

---

## 部署配置

### GitHub Pages 部署

**文件位置：** `.github/workflows/hugo.yaml`

这个文件已经配置好了，推送到 `master` 或 `main` 时会自动部署。

**需要修改的地方：**

1. **更新 baseURL**（在 `hugo.toml`）：
   ```toml
   baseURL = "https://yourusername.github.io/"
   ```

2. **GitHub 仓库设置：**
   - 进入仓库 Settings → Pages
   - Source 选择 "GitHub Actions"

### 本地预览

```bash
# 启动开发服务器
hugo server -D

# 访问 http://localhost:1313
```

### 构建静态网站

```bash
# 生成静态文件到 public/ 目录
hugo

# 不包含草稿
hugo --minify
```

---

## 高级配置

### 代码高亮主题

**文件位置：** `hugo.toml`

```toml
[markup.highlight]
style = "dracula"    # 代码高亮主题
```

**可选主题：**
- `dracula` - 深色主题
- `monokai` - 经典深色
- `github` - GitHub 风格
- `solarized-dark` - Solarized 深色
- `solarized-light` - Solarized 浅色

查看所有主题：https://xyproto.github.io/splash/docs/

### 内容类型配置

```toml
mainSections = ["posts", "notes", "papers"]    # 首页显示的内容类型
```

**修改首页显示内容：**
```toml
mainSections = ["posts"]    # 只显示博文
```

### 分类法配置

```toml
[taxonomies]
category = "categories"    # 分类
tag = "tags"              # 标签
series = "series"         # 系列
```

---

## 快速参考

### 常用命令

```bash
# 启动开发服务器
hugo server -D

# 创建新文章
hugo new posts/my-post.md

# 构建网站
hugo

# 查看 Hugo 版本
hugo version
```

### 常用文件路径

```
配置文件:        hugo.toml
自定义样式:      static/css/custom.css
侧边栏布局:      layouts/partials/sidebar.html
顶部导航:        layouts/partials/navbar.html
内容目录:        content/
静态资源:        static/
主题文件:        themes/heyo/
```

### 修改流程

1. 修改配置文件或内容
2. 保存文件
3. Hugo 自动重新加载（如果服务器在运行）
4. 刷新浏览器查看效果
5. 满意后提交到 Git

---

## 常见问题

### Q: 修改后没有生效？

**A:** 尝试以下方法：
1. 强制刷新浏览器（Ctrl + F5）
2. 清除浏览器缓存
3. 重启 Hugo 服务器
4. 检查文件是否保存

### Q: 如何更改网站语言？

**A:** 修改 `hugo.toml`：
```toml
languageCode = "en"              # 英文
defaultContentLanguage = "en"
```

### Q: 如何添加评论系统？

**A:** 当前仓库没有接入评论系统。如果需要添加，建议在 `layouts/` 下新增对应 partial 或覆盖 `single.html`，再按所选服务的要求补充配置。

### Q: 数学公式不显示？

**A:** 确保：
1. `hugo.toml` 中 `params.math.enable = true`
2. 文章 front matter 中有 `math: true`
3. 使用正确的公式语法（`$...$` 或 `$$...$$`）

---

## 更多资源

- [Hugo 官方文档](https://gohugo.io/documentation/)
- [heyo 主题仓库](https://github.com/LucasVadilho/heyo-hugo-theme)
- [Markdown 语法指南](https://www.markdownguide.org/)
- [Font Awesome 图标](https://fontawesome.com/icons)

---

**最后更新：** 2026-04-10
